# MangoWC Configuration
# Documentation: https://mangowm.github.io/docs/configuration/basics

# Modifiers: SUPER, ALT, CTRL, SHIFT

# Monitor Configuration
# monitorrule=name:REGEX,width:W,height:H,refresh:R,x:X,y:Y,scale:S
monitorrule=name:^DP-1$,width:2560,height:1440,refresh:119.998,x:0,y:0,scale:1
monitorrule=name:^DP-2$,width:1920,height:1080,refresh:60,x:2560,y:0,scale:1

# Environment Variables
env=XDG_CURRENT_DESKTOP,mango
env=XDG_SESSION_TYPE,wayland

# Keyboard Settings
xkb_rules_layout=us
xkb_rules_variant=intl
repeat_rate=40
repeat_delay=300
numlockon=1

# Mouse Settings
mouse_accel_profile=1
mouse_accel_speed=0.0
mouse_natural_scrolling=0

# Trackpad Settings
tap_to_click=1
trackpad_natural_scrolling=1
trackpad_accel_profile=2

# Basic Binds
# bind=<MODIFIERS>,<KEY>,<ACTION>,<PARAMETERS>
bind=SUPER,Return,spawn,foot
bind=SUPER,Space,spawn,fuzzel
bind=SUPER,q,killclient,
bind=SUPER+SHIFT,r,reload_config
bind=SUPER+SHIFT,e,quit,

# Layouts
bind=SUPER,t,setlayout,tiled
bind=SUPER,f,setlayout,fullscreen
bind=SUPER,m,setlayout,monocle
bind=SUPER,s,togglefloating,

# Navigation (Vim-like)
bind=SUPER,j,focusstack,1
bind=SUPER,k,focusstack,-1
bind=SUPER,h,setmfact,-0.05
bind=SUPER,l,setmfact,0.05

# Window resizing/moving (Floating mode)
bind=SUPER,Left,move,x=-20
bind=SUPER,Right,move,x=20
bind=SUPER,Up,move,y=-20
bind=SUPER,Down,move,y=20

# Workspaces (Tags in dwl-speak)
bind=SUPER,1,view,1
bind=SUPER,2,view,2
bind=SUPER,3,view,3
bind=SUPER,4,view,4
bind=SUPER,5,view,5
bind=SUPER,6,view,6
bind=SUPER,7,view,7
bind=SUPER,8,view,8
bind=SUPER,9,view,9

bind=SUPER+SHIFT,1,tag,1
bind=SUPER+SHIFT,2,tag,2
bind=SUPER+SHIFT,3,tag,3
bind=SUPER+SHIFT,4,tag,4
bind=SUPER+SHIFT,5,tag,5
bind=SUPER+SHIFT,6,tag,6
bind=SUPER+SHIFT,7,tag,7
bind=SUPER+SHIFT,8,tag,8
bind=SUPER+SHIFT,9,tag,9

# Aesthetics
gap_size=10
border_size=2
active_border_color=0x7f5bccaa
inactive_border_color=0x505050ff

# Autostart
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once=blueman-applet
exec-once=vesktop
